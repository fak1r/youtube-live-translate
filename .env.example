# Optional. If omitted or empty, the app connects directly and your system VPN
# handles routing.

# Optional. If set, the app starts the built-in xray sidecar and routes
# YouTube requests through this proxy URL. Supports both `vless://...` and
# `ss://...`.
# PROXY=vless://...

# Optional.
# LOG_LEVEL=debug
